bundle:
  name: faers_datalakehouse
  uuid: ec0d7b2d-9cfc-4c8b-af1c-a13bea9b7e4f

variables:
  workspace_host:
    description: "Databricks workspace URL"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${workspace_host}

    presets:
      artifacts_dynamic_version: true

  prod:
    mode: production
    workspace:
      host: ${workspace_host}
      root_path: /Workspace/Users/m.leon.kaba@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: m.leon.kaba@gmail.com
        level: CAN_MANAGE
